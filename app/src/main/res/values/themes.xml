<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Material3 DayNight base: works with enableEdgeToEdge() on API 26+ and
         lets system bar icon tint follow the window flags. Compose drives the
         actual UI theme via KudosAppTheme. -->
    <style name="Theme.KudosApp" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>